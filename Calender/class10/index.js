const{createApp} = Vue;

        createApp({
            data(){
                return{
                    message : "helllow my name is song ha gyue"
                }
            }
        }).mount("#app")/*뷰객체를 생성해서 div에다가 mount(안에 get elemnent By Id가 있음)*/